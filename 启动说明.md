# 新闻图片生成系统 - 启动说明

## 快速启动

### 方法一：自动启动（推荐）
```bash
npm start
```
系统会自动检查依赖并启动服务器。

### 方法二：开发模式
```bash
npm run dev
```
使用nodemon自动重启，适合开发调试。

### 方法三：手动检查依赖
```bash
npm run check-deps
```
只检查依赖，不启动服务器。

## 访问地址

启动成功后，您可以通过以下地址访问：

- **首页**: http://localhost:3000
- **后台配置**: http://localhost:3000/admin
- **新闻图片API**: http://localhost:3000/api/news-image

## 使用步骤

### 1. 配置OpenAI API
1. 访问 http://localhost:3000/admin
2. 填写您的OpenAI API地址、Key和模型名称
3. 设置关键词（用逗号分隔，如：科技,社会,财经）
4. 点击"保存配置"

### 2. 生成新闻图片
1. 在后台页面点击"测试生成"
2. 或直接访问 http://localhost:3000/api/news-image
3. 系统会自动调用OpenAI API获取新闻并生成图片

### 3. 获取图片
- 生成的图片保存在 `public/news-latest.html`
- 可以通过 http://localhost:3000/news-latest.html 访问
- 每次请求都会更新为最新的新闻内容

## 功能特性

- ✅ 自动依赖检测和安装
- ✅ 可视化后台配置
- ✅ OpenAI API集成
- ✅ 实时新闻图片生成
- ✅ 固定URL返回最新图片
- ✅ 模型身份统一回答

## 注意事项

1. **首次启动**：系统会自动安装缺失的依赖，请耐心等待
2. **API配置**：请确保OpenAI API地址和Key正确
3. **网络连接**：需要稳定的网络连接访问OpenAI API
4. **配置文件**：配置保存在 `config.json` 文件中

## 故障排除

### 依赖安装失败
- 检查网络连接
- 尝试切换npm源：`npm config set registry https://registry.npmmirror.com`
- 手动安装：`npm install`

### API调用失败
- 检查OpenAI API配置是否正确
- 确认API Key有效且有足够余额
- 检查网络连接

### 图片生成失败
- 检查public目录是否存在
- 确认有写入权限
- 查看服务器日志

## 部署说明

### 本地部署
直接运行 `npm start` 即可。

### 云端部署
1. 上传所有文件到云服务器
2. 运行 `npm install` 安装依赖
3. 运行 `npm start` 启动服务
4. 配置反向代理（如nginx）指向3000端口

### 环境变量
可以通过环境变量配置端口：
```bash
PORT=8080 npm start
``` 